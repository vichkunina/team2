'use strict';

const UserModel = require('./User');
const ChatModel = require('./Chat');
const MessageModel = require('./Message');

module.exports = {
    UserModel,
    ChatModel,
    MessageModel
};
